import '../styles/globals.css';

import { AppProps } from 'next/app';

// TODO: Fix this error below and add prettierconfig

function App({ Component, pageProps }: AppProps) {
  return <Component {...pageProps} />;
}

export default App;
